import React from "react"
import { Link } from "react-router"
import "../styles/splash.css"

const Splash = () => {
    return(
        <main id="splash">
            <section id="logo">
                ProRepo
            </section>
        </main>
    )
};

export default Splash;