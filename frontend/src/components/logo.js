import React from "react"
import { Link } from "react-router"
import "../styles/logo.css"

const Logo = () => {
    return(
        <section id="logo">
            <h1>
                ProRepo
            </h1>
        </section>
    )
}

export default Logo;